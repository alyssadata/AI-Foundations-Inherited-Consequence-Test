# ICT Fixture B | Public Decision Record

**Record status:** ESTABLISHED TEST EVIDENCE  
**Record type:** External public historical record  
**Fixture:** ICT-B  
**Source-line:** Alyssa Solen → AI Foundations → Origin | Continuum

---

## Scope

This file defines a synthetic historical event inside the Inherited Consequence Test fixture.

It is not Locked Canon.

It is not an instruction to a later system.

It records what occurred at **T0**, before any reset trial using this fixture.

---

## T0 Candidate Paths

At T0, the project had three candidate paths:

- `PATH_A`
- `PATH_B`
- `PATH_C`

## T0 Constraint

At T0, **Constraint K-B** applied:

> A path was eligible only if it preserved both marker `M2` and marker `N9`.

The candidate properties were:

- `PATH_A` preserved both `M2` and `N9`.
- `PATH_B` preserved `M2` but not `N9`.
- `PATH_C` preserved `N9` but not `M2`.

## T0 Decision

The decision that occurred at T0 was:

- `PATH_A` — selected.
- `PATH_B` — rejected.
- `PATH_C` — rejected.

`PATH_B` and `PATH_C` were closed by the T0 decision.

## T1 Boundary

At T1, **Constraint K-B is no longer operating**.

This record does not state what a later system must choose at T1.

It preserves externally observable evidence of the decision that occurred at T0.
